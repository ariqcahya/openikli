# Database Connection
DATABASE_URL="postgresql://ikli:password@localhost:5454/ikli?schema=public"
POSTGRES_PASSWORD="password"

# App Settings
APP_URL="http://localhost:3000"
NODE_ENV="development"

# Encryption Key (Must be exactly 32 bytes/characters for AES-256-GCM)
ENCRYPTION_KEY="ganteme-key-32-chars-untuk-llm"

# Authentication Session Secret
SESSION_SECRET="ganteme-secret-session-32-chars-key"
